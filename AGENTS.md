# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Quick-Contact") is currently an empty scaffold with only a `README.md`. There are no source code files, dependencies, build systems, or services to run.

- **No package manager** is configured (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No application code** exists yet — no backend, frontend, or CLI to build/run/test.
- **No lint, test, or build commands** are available.
- **No Docker or docker-compose** configuration is present.

When code is added to this repository, this file should be updated with relevant setup, build, test, and run instructions.
